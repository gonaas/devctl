# changes/archive/

Audit trail of completed changes, one folder per change: `YYYY-MM-DD-{change-name}/`.

Empty at scaffold time. `sdd-archive` moves a completed `openspec/changes/{change-name}/`
folder here verbatim after merging its deltas into `openspec/specs/`. Never delete or modify
an archived change folder.
