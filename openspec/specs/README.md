# specs/

Source of truth for main specs, organized by domain: `specs/{domain}/spec.md`.

Empty at scaffold time — no domain specs exist yet. `sdd-archive` populates this directory
by merging accepted change deltas (`openspec/changes/{change-name}/specs/{domain}/spec.md`)
into the corresponding `specs/{domain}/spec.md` file.
